# Turn on validation, so we can use import:
version: ~> 1.0

services:
- redis
- postgresql
- yarn

before_script:
- brew install libidn

script:
- export RAILS_ENV=test
- bundle install
- rails db:setup
- ./bin/rails assets:precompile
- bundle exec rspec
